databaseChangeLog:
  - changeSet:
      id: create-rolle-table
      author: artemmakovskyy
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: enum('ROLE_ADMIN','ROLE_MANAGER','ROLE_CUSTOMER')
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: is_deleted
                  type: boolean
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
                    unique: false