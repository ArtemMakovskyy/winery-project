server.port=${port:8080}

spring.jackson.deserialization.fail-on-unknown-properties=true
server.servlet.context-path=/api
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
security.paths=/auth/**,/error/**,/health/**,/wines/**,/reviews/**,/orders/**,/images/**
jwt.expiration=86400000

limiter.number.of.recorded.ratings=100
main.back.end.url=http://localhost:8080

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.file-size-threshold=50KB

jwt.secret=winestoreapi13112023secretkey123456789012
bot.name=input Wine Store bot name
bot.token=input Wine Store bot token
telegram.bot.enabled=false

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.datasource.url=${datasource.url:jdbc:mysql://localhost:3309/wine_store_db?serverTimezone=UTC}
spring.datasource.username=${datasource.user:userName}
spring.datasource.password=${datasource.password:secret}
